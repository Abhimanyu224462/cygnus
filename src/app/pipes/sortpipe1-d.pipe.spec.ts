import { Sortpipe1DPipe } from './sortpipe1-d.pipe';

describe('Sortpipe1DPipe', () => {
  it('create an instance', () => {
    const pipe = new Sortpipe1DPipe();
    expect(pipe).toBeTruthy();
  });
});
